import React from "react";

function StatusIcon(props) {
    if (props.status == "ready") {
    }

    return <button style={{ width: "50px", height: "150px" }}>asdasd</button>;
}

export default StatusIcon;
